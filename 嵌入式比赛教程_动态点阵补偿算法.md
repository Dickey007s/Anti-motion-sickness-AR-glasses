# 嵌入式比赛教程：动态点阵补偿算法完整实现

> **项目**: Anti-motion-sickness AR Glasses (防晕车 AR 眼镜)  
> **目标**: 基于 ESP32-S3 + BMI270 + ST7789V3 实现动态点阵视觉补偿算法  
> **文档版本**: v1.0  
> **生成日期**: 2026-05-06

---

## 目录

1. [项目概述与硬件架构](#一项目概述与硬件架构)
2. [动态点阵算法原理](#二动态点阵算法原理)
3. [ST7789 显示驱动基础](#三st7789-显示驱动基础)
4. [BMI270 传感器数据采集](#四bmi270-传感器数据采集)
5. [动态点阵算法完整实现](#五动态点阵算法完整实现)
6. [代码整合与系统联调](#六代码整合与系统联调)
7. [性能优化与调试技巧](#七性能优化与调试技巧)
8. [常见问题排查](#八常见问题排查)

---

## 一、项目概述与硬件架构

### 1.1 项目背景

防晕车 AR 眼镜通过检测车辆运动状态，在显示屏上呈现与车辆加速度方向相反的动态点阵，利用视觉-前庭协调机制缓解晕车症状。

### 1.2 硬件清单

| 组件 | 型号 | 关键参数 |
|------|------|----------|
| 主控 | ESP32-S3 Super Mini | 240MHz 双核, 512KB SRAM |
| 运动传感器 | Bosch BMI270 | 6轴 IMU, I2C 接口, ±4g/±500°/s |
| 显示屏 | 1.47" ST7789V3 | 172×320 IPS, SPI, RGB565 |
| 电池 | 3.7V 锂聚合物 | 500mAh, 续航 2-3 小时 |

### 1.3 引脚分配

| GPIO | 功能 | 连接目标 |
|------|------|----------|
| GP6  | I2C SDA | BMI270 SDA |
| GP7  | I2C SCL | BMI270 SCL |
| GP8  | TFT RST | ST7789 RES |
| GP9  | TFT BL  | ST7789 背光 |
| GP10 | SPI CS  | ST7789 CS |
| GP11 | SPI MOSI| ST7789 SDA |
| GP12 | SPI SCK | ST7789 SCL |
| GP13 | TFT DC  | ST7789 DC |

### 1.4 通信协议

**I2C (BMI270)**:
- 速率: 400kHz (Fast Mode)
- 地址: 0x68 (SDO 接地)
- 必须外接 4.7kΩ 上拉电阻到 3.3V

**SPI (ST7789V3)**:
- 模式: Mode 0 (CPOL=0, CPHA=0)
- 速率: 10-40MHz (建议初版 10MHz)
- 数据格式: 8-bit 命令/数据混合

---

## 二、动态点阵算法原理

### 2.1 核心机制

动态点阵算法的核心思想是：**根据车辆的纵向加速度，在显示屏上生成一个反向移动的点阵图案，让视觉系统感知到与内耳前庭系统相反的运动信号，从而减轻感官冲突**。

### 2.2 算法流程图

```
┌─────────────┐    ┌─────────────┐    ┌─────────────┐
│  BMI270 原始  │ → │  零偏补偿与   │ → │  互补滤波    │
│  数据采集    │    │  低通滤波    │    │  姿态解算    │
└─────────────┘    └─────────────┘    └──────┬──────┘
                                               │
                    ┌─────────────┐    ┌───────▼───────┐
                    │  点阵绘制与   │ ← │  世界坐标系    │
                    │  TFT 显示    │    │  纵向加速度   │
                    └─────────────┘    └───────┬───────┘
                                               │
                    ┌─────────────┐    ┌───────▼───────┐
                    │  一阶平滑与   │ ← │  运动状态    │
                    │  限幅保护    │    │  检测与偏移   │
                    └─────────────┘    └───────────────┘
```

### 2.3 数学模型

#### 2.3.1 零偏补偿

上电后静止标定 2 秒，采样 200 组数据求平均值：

```
acc_bias_x = Σ(acc_raw_x) / 200
acc_bias_y = Σ(acc_raw_y) / 200
acc_bias_z = Σ(acc_raw_z) / 200

gyro_bias_x = Σ(gyro_raw_x) / 200
gyro_bias_y = Σ(gyro_raw_y) / 200
gyro_bias_z = Σ(gyro_raw_z) / 200
```

#### 2.3.2 低通滤波

```
a_filtered = 0.3 * a_raw + 0.7 * a_filtered_prev
```

#### 2.3.3 互补滤波姿态解算

```
pitch = 0.98 * (pitch + gyro_y * dt) + 0.02 * atan2(-acc_x, sqrt(acc_y² + acc_z²))
roll  = 0.98 * (roll  + gyro_x * dt) + 0.02 * atan2(acc_y, acc_z)
```

> `dt` 为采样周期，100Hz 时 `dt = 0.01s`。建议使用 `millis()` 差值计算真实 `dt`。

#### 2.3.4 世界坐标系纵向加速度

将传感器坐标系加速度转换到世界坐标系：

```
a_x_world = acc_x * cos(pitch) + acc_z * sin(pitch)
```

#### 2.3.5 运动状态检测

```
if a_x_world >  ACCEL_THRESHOLD (0.1g):  state = ACCELERATING
if a_x_world < -ACCEL_THRESHOLD (-0.1g): state = BRAKING
else:                                      state = CRUISING
```

#### 2.3.6 视觉补偿偏移量

```
target_offset = -K * a_x_world          // K = 20 px/g, 负号表示反向
offset = offset + alpha * (target_offset - offset)  // alpha = 0.15, 一阶平滑
offset = clamp(offset, -MAX_OFFSET, MAX_OFFSET)     // MAX_OFFSET = 12 px
```

### 2.4 点阵规格

- **布局**: 4 行 × 5 列
- **点间距**: 8 像素
- **点大小**: 4×4 像素 (fillRect 绘制)
- **颜色策略**:
  - 加速 (ACCELERATING): 红色 (0xF800)
  - 减速 (BRAKING): 绿色 (0x07E0)
  - 匀速 (CRUISING): 白色 (0xFFFF)

---

## 三、ST7789 显示驱动基础

### 3.1 硬件特性

| 参数 | 值 |
|------|-----|
| 分辨率 | 172(H) × 320(V) |
| 颜色格式 | RGB565 (16bit) |
| 接口 | SPI Mode 0 |
| 时钟 | 8-40MHz |
| 关键约束 | 172×320 为窄屏裁切，必须设置 CASET/RASET 偏移 |

### 3.2 172×320 窗口偏移（关键！）

ST7789V3 原生支持 240×320。1.47 寸 172×320 必须设置显示窗口：

```cpp
// CASET (Column Address Set): X 起始=34, X 结束=221
// 共 188 像素宽度，实际显示 172 像素
tft.sendCommand(0x2A);
uint8_t casetData[] = {0x00, 0x22, 0x00, 0xDD};
tft.writeData(casetData, 4);

// RASET (Row Address Set): Y 起始=0, Y 结束=319
tft.sendCommand(0x2B);
uint8_t rasetData[] = {0x00, 0x00, 0x01, 0x3F};
tft.writeData(rasetData, 4);
```

**换算公式**:
- 起始 X = (240 - 172) / 2 = 34 = 0x22
- 结束 X = 34 + 172 - 1 = 221 = 0xDD

### 3.3 RGB565 颜色格式

每个像素 16 bit：
- R: bit[15..11] (5 bit)
- G: bit[10..5]  (6 bit)
- B: bit[4..0]   (5 bit)

**自定义颜色**:
```cpp
// 方式1: 直接拼 RGB565
uint16_t myColor = (r << 11) | (g << 5) | b;   // r=0..31, g=0..63, b=0..31

// 方式2: 宏
#define RGB565(r, g, b) (((r & 0xF8) << 8) | ((g & 0xFC) << 3) | (b >> 3))
uint16_t orange = RGB565(255, 165, 0);
```

### 3.4 坐标系统

- 原点 `(0, 0)` 在屏幕左上角
- `x` 向右递增，范围 `0` ~ `width()-1`
- `y` 向下递增，范围 `0` ~ `height()-1`
- `_colStart` (34) 已在 `setAddressWindow` 中自动叠加

### 3.5 显存写入流程

```
1. setAddressWindow(x1, y1, x2, y2)  // 告诉屏幕更新区域
2. sendCommand(0x2C)                 // RAMWR: 开始写入显存
3. 连续 SPI 发送像素数据（每个像素 2 字节，高字节在前）
```

**关键结论**:
- 指定地址窗口后发 0x2C，可连续写像素，屏幕自动按行递增
- 写满窗口后自动停止，无需手动重置地址

### 3.6 标准初始化序列

```
1. RST 低电平 ≥ 10ms，然后拉高，等待 120ms
2. SLPOUT (0x11) — 退出睡眠，等待 120ms
3. COLMOD (0x3A) = 0x55 — 16bit 颜色 (RGB565)
4. MADCTL (0x36) = 0x00 — 扫描方向
5. CASET (0x2A) = [0x00, 0x22, 0x00, 0xDD] — 列地址窗口
6. RASET (0x2B) = [0x00, 0x00, 0x01, 0x3F] — 行地址窗口
7. INVON (0x21) 或 INVOFF (0x20) — 根据面板极性
8. DISPON (0x29) — 开启显示
```

---

## 四、BMI270 传感器数据采集

### 4.1 初始化流程（必须严格执行）

**步骤 1: 软复位**
```cpp
writeRegister(0x7E, 0xB6);  // CMD = 软复位
delay(2);
```

**步骤 2: 加载配置固件（关键！不可跳过）**
```cpp
// 从 Bosch 官方库获取 bmi270_config_file (~8KB)
// 从寄存器 0x5E (INIT_DATA_0) 开始连续写入
for (int i = 0; i < config_size; i++) {
    writeRegister(0x5E + (i % 256), config_file[i]);
}
writeRegister(0x7E, 0x11);  // 触发初始化

// 轮询 INTERNAL_STATUS (0x21) bit 0 = 1
while ((readRegister(0x21) & 0x01) == 0) {
    delay(1);
}
```

**步骤 3: 启用传感器**
```cpp
writeRegister(0x7E, 0x11);  // ACC_PWR_CTRL
writeRegister(0x7E, 0x15);  // GYR_PWR_CTRL
```

**步骤 4: 配置量程与 ODR**
```cpp
// 加速度计: 100Hz, ±4g
writeRegister(0x40, 0x0A);  // ACC_CONF: ODR = 100Hz
writeRegister(0x41, 0x03);  // ACC_RANGE: ±4g

// 陀螺仪: 100Hz, ±500°/s
writeRegister(0x42, 0x0A);  // GYR_CONF: ODR = 100Hz
writeRegister(0x43, 0x01);  // GYR_RANGE: ±500°/s
```

### 4.2 数据读取

**寄存器**: 0x0C ~ 0x17 (DATA_0 ~ DATA_23)，共 24 字节

**数据布局**（小端）:
```
0x0C-0x0D: ACC_X  (int16, LSB = 0.000122 g @ ±4g)
0x0E-0x0F: ACC_Y
0x10-0x11: ACC_Z
0x12-0x13: GYR_X  (int16, LSB = 0.061 °/s @ ±2000°/s)
0x14-0x15: GYR_Y
0x16-0x17: GYR_Z
```

### 4.3 常用寄存器速查

| 寄存器 | 地址 | 功能 |
|--------|------|------|
| CHIP_ID | 0x00 | 芯片 ID = 0x24 |
| ERR_REG | 0x02 | 错误标志 |
| STATUS | 0x03 | 数据就绪状态 |
| ACC_CONF | 0x40 | 加速度计配置 |
| ACC_RANGE | 0x41 | 加速度计量程 |
| GYR_CONF | 0x42 | 陀螺仪配置 |
| GYR_RANGE | 0x43 | 陀螺仪量程 |
| INIT_DATA_0 | 0x5E | 配置固件加载入口 |
| INTERNAL_STATUS | 0x21 | 初始化状态 |
| CMD | 0x7E | 命令寄存器 |

---

## 五、动态点阵算法完整实现

### 5.1 算法架构

```cpp
// ===================== 数据结构定义 =====================

// 运动状态枚举
enum MotionState {
    CRUISING = 0,      // 匀速巡航
    ACCELERATING = 1,  // 加速
    BRAKING = 2        // 减速
};

// 点阵参数
#define GRID_ROWS       4       // 行数
#define GRID_COLS       5       // 列数
#define DOT_SIZE        4       // 点大小 (像素)
#define DOT_SPACING     8       // 点间距 (像素)
#define GRID_OFFSET_X   66      // 点阵起始 X (居中: (172-5*8-4)/2 ≈ 66)
#define GRID_OFFSET_Y   136     // 点阵起始 Y (居中: (320-4*8-4)/2 ≈ 136)

// 算法参数
#define K               20.0f     // 增益: px/g
#define ALPHA           0.15f    // 平滑系数
#define MAX_OFFSET      12       // 最大偏移 (像素)
#define ACCEL_THRESHOLD 0.1f     // 加速度阈值 (g)

// 滤波参数
#define LPF_ALPHA       0.3f     // 低通滤波系数

// ===================== 全局变量 =====================

// 零偏
float acc_bias[3] = {0, 0, 0};
float gyro_bias[3] = {0, 0, 0};

// 低通滤波前值
float acc_filtered_prev[3] = {0, 0, 0};

// 姿态角
float pitch = 0.0f;
float roll = 0.0f;

// 偏移量
float offset = 0.0f;

// 运动状态
MotionState motionState = CRUISING;

// 时间戳
unsigned long lastTime = 0;
```

### 5.2 零偏标定

```cpp
void calibrateSensors() {
    Serial.println("开始零偏标定，请保持静止...");
    
    float acc_sum[3] = {0, 0, 0};
    float gyro_sum[3] = {0, 0, 0};
    const int samples = 200;
    
    for (int i = 0; i < samples; i++) {
        bmi.getSensorData();
        
        acc_sum[0] += bmi.data.accelX;
        acc_sum[1] += bmi.data.accelY;
        acc_sum[2] += bmi.data.accelZ;
        
        gyro_sum[0] += bmi.data.gyroX;
        gyro_sum[1] += bmi.data.gyroY;
        gyro_sum[2] += bmi.data.gyroZ;
        
        delay(10);  // 100Hz 采样
    }
    
    // 计算平均值
    for (int i = 0; i < 3; i++) {
        acc_bias[i] = acc_sum[i] / samples;
        gyro_bias[i] = gyro_sum[i] / samples;
    }
    
    Serial.printf("零偏标定完成: ACC=[%.3f, %.3f, %.3f], GYRO=[%.3f, %.3f, %.3f]\n",
                  acc_bias[0], acc_bias[1], acc_bias[2],
                  gyro_bias[0], gyro_bias[1], gyro_bias[2]);
}
```

### 5.3 低通滤波

```cpp
void applyLowPassFilter(float acc_raw[3], float acc_filtered[3]) {
    for (int i = 0; i < 3; i++) {
        acc_filtered[i] = LPF_ALPHA * acc_raw[i] + (1.0f - LPF_ALPHA) * acc_filtered_prev[i];
        acc_filtered_prev[i] = acc_filtered[i];
    }
}
```

### 5.4 互补滤波姿态解算

```cpp
void updateAttitude(float acc[3], float gyro[3], float dt) {
    // 陀螺仪积分 (转换为弧度)
    float gyro_rad[3] = {
        gyro[0] * DEG_TO_RAD,
        gyro[1] * DEG_TO_RAD,
        gyro[2] * DEG_TO_RAD
    };
    
    // 加速度计计算姿态角
    float acc_pitch = atan2(-acc[0], sqrt(acc[1] * acc[1] + acc[2] * acc[2]));
    float acc_roll  = atan2(acc[1], acc[2]);
    
    // 互补滤波
    pitch = 0.98f * (pitch + gyro_rad[1] * dt) + 0.02f * acc_pitch;
    roll  = 0.98f * (roll  + gyro_rad[0] * dt) + 0.02f * acc_roll;
}
```

### 5.5 世界坐标系纵向加速度

```cpp
float extractLongitudinalAcceleration(float acc[3]) {
    // a_x_world = acc_x * cos(pitch) + acc_z * sin(pitch)
    float a_x_world = acc[0] * cos(pitch) + acc[2] * sin(pitch);
    return a_x_world;
}
```

### 5.6 运动状态检测

```cpp
MotionState detectMotionState(float a_x_world) {
    if (a_x_world > ACCEL_THRESHOLD) {
        return ACCELERATING;
    } else if (a_x_world < -ACCEL_THRESHOLD) {
        return BRAKING;
    } else {
        return CRUISING;
    }
}
```

### 5.7 视觉补偿偏移量计算

```cpp
float calculateVisualOffset(float a_x_world) {
    // target_offset = -K * a_x_world (负号表示反向)
    float target_offset = -K * a_x_world;
    
    // 一阶平滑滤波
    offset = offset + ALPHA * (target_offset - offset);
    
    // 限幅保护
    if (offset > MAX_OFFSET) offset = MAX_OFFSET;
    if (offset < -MAX_OFFSET) offset = -MAX_OFFSET;
    
    return offset;
}
```

### 5.8 点阵绘制

```cpp
void drawDotGrid(float offset, MotionState state) {
    // 选择颜色
    uint16_t color;
    switch (state) {
        case ACCELERATING: color = ST77XX_RED;    break;
        case BRAKING:      color = ST77XX_GREEN;  break;
        case CRUISING:     color = ST77XX_WHITE;   break;
        default:           color = ST77XX_WHITE;
    }
    
    // 清屏 (深色背景)
    tft.fillScreen(ST77XX_BLACK);
    
    // 绘制 4×5 点阵
    for (int row = 0; row < GRID_ROWS; row++) {
        for (int col = 0; col < GRID_COLS; col++) {
            // 计算点位置 (加入偏移量)
            int16_t x = GRID_OFFSET_X + col * DOT_SPACING + (int16_t)offset;
            int16_t y = GRID_OFFSET_Y + row * DOT_SPACING;
            
            // 边界检查
            if (x < 0 || x + DOT_SIZE > SCREEN_W) continue;
            if (y < 0 || y + DOT_SIZE > SCREEN_H) continue;
            
            // 绘制点
            tft.fillRect(x, y, DOT_SIZE, DOT_SIZE, color);
        }
    }
}
```

### 5.9 主循环整合

```cpp
void loop() {
    unsigned long currentTime = millis();
    float dt = (currentTime - lastTime) / 1000.0f;
    lastTime = currentTime;
    
    // 1. 读取传感器数据
    bmi.getSensorData();
    
    // 2. 零偏补偿
    float acc_raw[3] = {
        bmi.data.accelX - acc_bias[0],
        bmi.data.accelY - acc_bias[1],
        bmi.data.accelZ - acc_bias[2]
    };
    float gyro_raw[3] = {
        bmi.data.gyroX - gyro_bias[0],
        bmi.data.gyroY - gyro_bias[1],
        bmi.data.gyroZ - gyro_bias[2]
    };
    
    // 3. 低通滤波
    float acc_filtered[3];
    applyLowPassFilter(acc_raw, acc_filtered);
    
    // 4. 互补滤波姿态解算
    updateAttitude(acc_filtered, gyro_raw, dt);
    
    // 5. 世界坐标系纵向加速度
    float a_x_world = extractLongitudinalAcceleration(acc_filtered);
    
    // 6. 运动状态检测
    motionState = detectMotionState(a_x_world);
    
    // 7. 视觉补偿偏移量
    float visualOffset = calculateVisualOffset(a_x_world);
    
    // 8. 绘制点阵
    drawDotGrid(visualOffset, motionState);
    
    // 9. 调试输出 (每 10 帧打印一次)
    static int frameCount = 0;
    if (++frameCount % 10 == 0) {
        Serial.printf("State=%d, a_x=%.3f, offset=%.1f, pitch=%.2f\n",
                      motionState, a_x_world, visualOffset, pitch);
    }
    
    // 10. 帧率控制 (100Hz)
    delay(10);
}
```

---

## 六、代码整合与系统联调

### 6.1 完整 main.cpp 结构

```cpp
#include <Arduino.h>
#include <SPI.h>
#include <Wire.h>
#include <Adafruit_GFX.h>
#include <Adafruit_ST7789.h>
#include <SparkFun_BMI270_Arduino_Library.h>

// ===================== 引脚定义 =====================
#define TFT_CS   10
#define TFT_DC   13
#define TFT_RST  8
#define TFT_BL   9

#define BMI_SDA   6
#define BMI_SCL   7
#define BMI_INT   1

// ===================== 显示参数 =====================
#define SCREEN_W  172
#define SCREEN_H  320

// ===================== 点阵参数 =====================
#define GRID_ROWS       4
#define GRID_COLS       5
#define DOT_SIZE        4
#define DOT_SPACING     8
#define GRID_OFFSET_X   66
#define GRID_OFFSET_Y   136

// ===================== 算法参数 =====================
#define K               20.0f
#define ALPHA           0.15f
#define MAX_OFFSET      12
#define ACCEL_THRESHOLD 0.1f
#define LPF_ALPHA       0.3f

// ===================== TFT 驱动 =====================
Adafruit_ST7789 tft = Adafruit_ST7789(TFT_CS, TFT_DC, TFT_RST);

// ===================== BMI270 驱动 =====================
BMI270 bmi;
const uint8_t BMI_ADDR = BMI2_I2C_PRIM_ADDR;

// ===================== 全局变量 =====================
float acc_bias[3] = {0};
float gyro_bias[3] = {0};
float acc_filtered_prev[3] = {0};
float pitch = 0.0f;
float roll = 0.0f;
float offset = 0.0f;
unsigned long lastTime = 0;

enum MotionState { CRUISING, ACCELERATING, BRAKING };
MotionState motionState = CRUISING;

// ===================== 函数声明 =====================
void calibrateSensors();
void applyLowPassFilter(float acc_raw[3], float acc_filtered[3]);
void updateAttitude(float acc[3], float gyro[3], float dt);
float extractLongitudinalAcceleration(float acc[3]);
MotionState detectMotionState(float a_x_world);
float calculateVisualOffset(float a_x_world);
void drawDotGrid(float offset, MotionState state);

// ===================== setup() =====================
void setup() {
    Serial.begin(115200);
    while (!Serial) { delay(10); }
    delay(300);
    
    // SPI 初始化
    SPI.begin(12, -1, 11, -1);
    
    // TFT 初始化
    tft.init(SCREEN_W, SCREEN_H);
    tft.sendCommand(0x11);
    delay(120);
    tft.sendCommand(0x29);
    delay(20);
    tft.setRotation(0);
    
    // 背光
    pinMode(TFT_BL, OUTPUT);
    digitalWrite(TFT_BL, HIGH);
    
    // I2C 初始化
    Wire.begin(BMI_SDA, BMI_SCL);
    
    // BMI270 初始化
    int8_t bmiStatus = bmi.beginI2C(BMI_ADDR);
    if (bmiStatus != BMI2_OK) {
        Serial.printf("BMI270 初始化失败: %d\n", bmiStatus);
    } else {
        Serial.println("BMI270 连接成功");
        
        // 配置加速度计
        bmi2_sens_config accConfig;
        accConfig.type = BMI2_ACCEL;
        accConfig.cfg.acc.odr = BMI2_ACC_ODR_100HZ;
        accConfig.cfg.acc.range = BMI2_ACC_RANGE_4G;
        accConfig.cfg.acc.bwp = BMI2_ACC_NORMAL_AVG4;
        accConfig.cfg.acc.filter_perf = BMI2_PERF_OPT_MODE;
        bmi.setConfig(accConfig);
        
        // 配置陀螺仪
        bmi2_sens_config gyrConfig;
        gyrConfig.type = BMI2_GYRO;
        gyrConfig.cfg.gyr.odr = BMI2_GYR_ODR_100HZ;
        gyrConfig.cfg.gyr.bwp = BMI2_GYR_NORMAL_MODE;
        gyrConfig.cfg.gyr.filter_perf = BMI2_PERF_OPT_MODE;
        bmi.setConfig(gyrConfig);
        
        // 启用传感器
        uint8_t sensList[2] = {BMI2_ACCEL, BMI2_GYRO};
        bmi.enableFeatures(sensList, 2);
        
        Serial.println("BMI270 配置完成");
        
        // 零偏标定
        calibrateSensors();
    }
    
    pinMode(BMI_INT, INPUT);
    lastTime = millis();
}

// ===================== loop() =====================
void loop() {
    unsigned long currentTime = millis();
    float dt = (currentTime - lastTime) / 1000.0f;
    lastTime = currentTime;
    
    // 读取传感器
    bmi.getSensorData();
    
    // 零偏补偿
    float acc_raw[3] = {
        bmi.data.accelX - acc_bias[0],
        bmi.data.accelY - acc_bias[1],
        bmi.data.accelZ - acc_bias[2]
    };
    float gyro_raw[3] = {
        bmi.data.gyroX - gyro_bias[0],
        bmi.data.gyroY - gyro_bias[1],
        bmi.data.gyroZ - gyro_bias[2]
    };
    
    // 低通滤波
    float acc_filtered[3];
    applyLowPassFilter(acc_raw, acc_filtered);
    
    // 姿态解算
    updateAttitude(acc_filtered, gyro_raw, dt);
    
    // 纵向加速度
    float a_x_world = extractLongitudinalAcceleration(acc_filtered);
    
    // 运动状态
    motionState = detectMotionState(a_x_world);
    
    // 偏移量
    float visualOffset = calculateVisualOffset(a_x_world);
    
    // 绘制
    drawDotGrid(visualOffset, motionState);
    
    // 调试
    static int frameCount = 0;
    if (++frameCount % 10 == 0) {
        Serial.printf("State=%d, a_x=%.3f, offset=%.1f\n",
                      motionState, a_x_world, visualOffset);
    }
    
    delay(10);
}
```

### 6.2 关键函数实现

```cpp
void calibrateSensors() {
    Serial.println("零偏标定中...");
    float acc_sum[3] = {0}, gyro_sum[3] = {0};
    const int samples = 200;
    
    for (int i = 0; i < samples; i++) {
        bmi.getSensorData();
        acc_sum[0] += bmi.data.accelX;
        acc_sum[1] += bmi.data.accelY;
        acc_sum[2] += bmi.data.accelZ;
        gyro_sum[0] += bmi.data.gyroX;
        gyro_sum[1] += bmi.data.gyroY;
        gyro_sum[2] += bmi.data.gyroZ;
        delay(10);
    }
    
    for (int i = 0; i < 3; i++) {
        acc_bias[i] = acc_sum[i] / samples;
        gyro_bias[i] = gyro_sum[i] / samples;
    }
    
    Serial.println("标定完成");
}

void applyLowPassFilter(float acc_raw[3], float acc_filtered[3]) {
    for (int i = 0; i < 3; i++) {
        acc_filtered[i] = LPF_ALPHA * acc_raw[i] + 
                          (1.0f - LPF_ALPHA) * acc_filtered_prev[i];
        acc_filtered_prev[i] = acc_filtered[i];
    }
}

void updateAttitude(float acc[3], float gyro[3], float dt) {
    float gyro_rad[3] = {
        gyro[0] * DEG_TO_RAD,
        gyro[1] * DEG_TO_RAD,
        gyro[2] * DEG_TO_RAD
    };
    
    float acc_pitch = atan2(-acc[0], sqrt(acc[1]*acc[1] + acc[2]*acc[2]));
    float acc_roll  = atan2(acc[1], acc[2]);
    
    pitch = 0.98f * (pitch + gyro_rad[1] * dt) + 0.02f * acc_pitch;
    roll  = 0.98f * (roll  + gyro_rad[0] * dt) + 0.02f * acc_roll;
}

float extractLongitudinalAcceleration(float acc[3]) {
    return acc[0] * cos(pitch) + acc[2] * sin(pitch);
}

MotionState detectMotionState(float a_x_world) {
    if (a_x_world > ACCEL_THRESHOLD) return ACCELERATING;
    if (a_x_world < -ACCEL_THRESHOLD) return BRAKING;
    return CRUISING;
}

float calculateVisualOffset(float a_x_world) {
    float target_offset = -K * a_x_world;
    offset = offset + ALPHA * (target_offset - offset);
    if (offset > MAX_OFFSET) offset = MAX_OFFSET;
    if (offset < -MAX_OFFSET) offset = -MAX_OFFSET;
    return offset;
}

void drawDotGrid(float offset, MotionState state) {
    uint16_t color;
    switch (state) {
        case ACCELERATING: color = ST77XX_RED;    break;
        case BRAKING:      color = ST77XX_GREEN;  break;
        default:           color = ST77XX_WHITE;
    }
    
    tft.fillScreen(ST77XX_BLACK);
    
    for (int row = 0; row < GRID_ROWS; row++) {
        for (int col = 0; col < GRID_COLS; col++) {
            int16_t x = GRID_OFFSET_X + col * DOT_SPACING + (int16_t)offset;
            int16_t y = GRID_OFFSET_Y + row * DOT_SPACING;
            
            if (x < 0 || x + DOT_SIZE > SCREEN_W) continue;
            if (y < 0 || y + DOT_SIZE > SCREEN_H) continue;
            
            tft.fillRect(x, y, DOT_SIZE, DOT_SIZE, color);
        }
    }
}
```

---

## 七、性能优化与调试技巧

### 7.1 性能瓶颈分析

| 瓶颈 | 当前状态 | 优化方向 |
|------|----------|----------|
| SPI 频率 | 10 MHz | 提升到 40 MHz |
| 逐像素画点 | fillRect 每次切换 CS | 批量写入或帧缓冲 |
| 全屏刷新 | fillScreen 逐字节 SPI | 局部刷新或双缓冲 |
| 浮点运算 | ESP32-S3 有 FPU | 少量浮点无压力 |
| 帧缓冲 | 无 | 可建 110KB 帧缓冲 |

### 7.2 帧缓冲优化

```cpp
// 全局帧缓冲 (110KB)
static uint16_t frameBuffer[172 * 320];

void drawDotGridOptimized(float offset, MotionState state) {
    uint16_t color = (state == ACCELERATING) ? ST77XX_RED :
                     (state == BRAKING) ? ST77XX_GREEN : ST77XX_WHITE;
    
    // 清屏
    memset(frameBuffer, 0, sizeof(frameBuffer));
    
    // 在帧缓冲上绘制
    for (int row = 0; row < GRID_ROWS; row++) {
        for (int col = 0; col < GRID_COLS; col++) {
            int16_t x = GRID_OFFSET_X + col * DOT_SPACING + (int16_t)offset;
            int16_t y = GRID_OFFSET_Y + row * DOT_SPACING;
            
            if (x < 0 || x + DOT_SIZE > SCREEN_W) continue;
            if (y < 0 || y + DOT_SIZE > SCREEN_H) continue;
            
            // 在帧缓冲上填充矩形
            for (int dy = 0; dy < DOT_SIZE; dy++) {
                for (int dx = 0; dx < DOT_SIZE; dx++) {
                    int px = x + dx;
                    int py = y + dy;
                    if (px >= 0 && px < SCREEN_W && py >= 0 && py < SCREEN_H) {
                        frameBuffer[py * SCREEN_W + px] = color;
                    }
                }
            }
        }
    }
    
    // 一次性刷到屏幕
    tft.setAddressWindow(0, 0, SCREEN_W-1, SCREEN_H-1);
    tft.sendCommand(0x2C);
    tft.writePixels((uint8_t*)frameBuffer, SCREEN_W * SCREEN_H * 2);
}
```

### 7.3 非阻塞帧率控制

```cpp
void loop() {
    static unsigned long lastFrame = 0;
    unsigned long now = millis();
    
    if (now - lastFrame >= 10) {  // 100Hz
        lastFrame = now;
        
        // 执行算法和绘制
        // ...
    }
    
    // 其他非阻塞任务
}
```

### 7.4 调试技巧

1. **串口打印**: 每 10 帧打印一次状态、加速度、偏移量
2. **LED 指示**: 用板载 WS2812 LED 指示运动状态
3. **颜色编码**: 加速红、减速绿、匀速白，直观判断算法状态
4. **静态测试**: 手持设备前后晃动，观察点阵移动方向是否正确

---

## 八、常见问题排查

### 8.1 TFT 花屏/错位

| 现象 | 原因 | 解决 |
|------|------|------|
| 图像横向错位 | CASET 偏移错误 | 确认 CASET = [0x00, 0x22, 0x00, 0xDD] |
| 颜色异常 | RGB565 字节序错误 | 确认高字节先发 |
| 屏幕不亮 | 背光未开启 | 检查 TFT_BL 引脚电平 |
| 图像倒置 | MADCTL 方向错误 | 尝试 0x00/0x40/0x80/0xC0 |

### 8.2 BMI270 无响应

| 现象 | 原因 | 解决 |
|------|------|------|
| I2C 扫描不到 | 地址错误或接线问题 | 扫描 0x68/0x69，检查接线 |
| CHIP_ID 错误 | 未加载配置固件 | 必须加载 8KB 配置固件 |
| 数据全 0 | 初始化未完成 | 轮询 INTERNAL_STATUS |
| 数据漂移 | 零偏未标定 | 上电静止标定 2 秒 |

### 8.3 算法异常

| 现象 | 原因 | 解决 |
|------|------|------|
| 静止时持续偏移 | 零偏标定错误 | 确保标定时设备静止 |
| 加减速检测反了 | 传感器安装方向错误 | 调整坐标系或修改符号 |
| 点阵抖动严重 | 平滑系数太小 | 增大 ALPHA 或降低 K |
| 响应迟钝 | 平滑系数太大 | 减小 ALPHA 或增大 K |
| 偏移量过大 | 限幅失效 | 检查 MAX_OFFSET 是否生效 |

### 8.4 系统性能

| 现象 | 原因 | 解决 |
|------|------|------|
| 帧率低 | SPI 速度慢 | 提升到 40MHz |
| 卡顿 | 全屏刷新开销大 | 使用帧缓冲或局部刷新 |
| 功耗高 | TFT 背光常亮 | 使用 PWM 调光 |
| 内存不足 | 栈上分配大数组 | 使用全局/静态变量 |

---

## 附录 A: 完整代码文件

完整代码见 `dynamic_dot_grid.cpp`，包含：
- 完整的 main.cpp 实现
- 所有算法函数
- 调试输出
- 性能优化版本

## 附录 B: 参考资源

- [BMI270-Sensor-API](https://github.com/boschsensortec/BMI270-Sensor-API)
- [ST7789V3 数据手册](https://www.displayfuture.com/Display/datasheet/controller/ST7789V3.pdf)
- [ESP32-S3 技术参考手册](https://docs.espressif.com/projects/esp-idf/en/latest/esp32s3/api-reference/index.html)

---

*文档生成日期: 2026-05-06*  
*基于项目: https://github.com/Dickey007s/Anti-motion-sickness-AR-glasses*
